import 'package:flutter/material.dart';

class MyCoursesPageWidget extends StatelessWidget {
  const MyCoursesPageWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}